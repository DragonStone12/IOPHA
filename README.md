
  # Healthcare Appointment Booking Flow

  This is a code bundle for Healthcare Appointment Booking Flow. The original project is available at https://www.figma.com/design/mE2fRu93wC8CEQ8wjoD04W/Healthcare-Appointment-Booking-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  